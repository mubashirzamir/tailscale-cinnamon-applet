{
    "poll_interval": {
        "type": "spinbutton",
        "default": 60,
        "min": 10,
        "max": 120,
        "step": 5,
        "description": "Seconds between Tailscale state refreshes."
    },
    "preferred_exit_node": {
        "type": "entry",
        "default": "",
        "description": "Tailscale exit node hostname. Leave empty for none."
    },
    "auto_connect_on_load": {
        "type": "switch",
        "default": false,
        "description": "Run tailscale up when the applet starts."
    },
    "show_debug": {
        "type": "switch",
        "default": false,
        "description": "Show debug log in the applet menu."
    }
}
